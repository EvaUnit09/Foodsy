package com.foodsy.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
    // CORS is handled by Nginx proxy
    // This class kept for potential local development without Nginx
}